# R5/M6.1 — LOCK_CANDIDATE v2

`M6.1 = LOCK_CANDIDATE`

Закрыты внешние блокеры `B-01…B-05` и мажорные замечания в границах M6.1.

Проверки:

- focused checker: `PASS`, 101 checks;
- role × target: `9/9` legal, `45/45` illegal rejected;
- schema definitions: `19`;
- fixtures: `39`;
- M1 Id sites: `88`, inline copies: `0`;
- v9 selftest: `PASS`;
- project regression tests: `107/107 PASS`.

Breaking interface transition требует `M0-integration-invariants: LOCKED -> REOPENED`. Нормативные файлы M0 не меняются.

Gate:

```text
M6.1=LOCKED
M6.1.frozen=false
focused_external_accept=false
M0=REOPENED
B1.1b=NOT_ACCEPTED
TASK-B1.1=NOT_ACCEPTED
B1.2=NOT_STARTED
SELECTOR_INSTANCES_FROZEN=0/20
NEXT=R5_M6_3_ENUMERATION_DOMAINS
```

Это внутренний `LOCK_CANDIDATE`, не внешний ACCEPT.
