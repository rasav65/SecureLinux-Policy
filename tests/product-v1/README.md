# product-v1 tests

`test_file_mode_owner_adapter.py` — механические тесты
`product/adapters/product-file-mode-owner-check-v1.py`:

- emitted bash не содержит мутирующих примитивов;
- fail-closed отказ для `owner` / `group` / `owner_group`, для op вне
  `eq` / `bits-clear`, для маски `0000`, для не-четырёхзначного значения,
  для относительного locator, для locator с кавычкой, переводом строки
  или подстановкой, для control_id вне `[A-Za-z0-9._-]`;
- связка adapter JSON <-> реализация <-> семантический контракт по SHA-256;
- поведение на фикстурах: VALUE (в том числе setuid-биты и режим 0000),
  NOT_FOUND только при доказанном отсутствии, ERROR для висячей ссылки,
  цикла ссылок, непроходимого родителя и отсутствия `stat`;
- целевой файл не изменяется.

Два теста непроходимого родителя пропускаются, если тесты запущены от root.

`test_sysctl_adapter.py` — механические тесты
`product/adapters/product-sysctl-check-v2.py` и `product/ADAPTER-REGISTRY.tsv`:

- отдельная product identity, historical `sysctl-check-v1` не изменяется;
- только `parameter.kind=sysctl`, `locator=sysctl`; current v2 поддерживает
  `eq` и source-faithful integer lower-bound `ge`;
- dotted sysctl key преобразуется в `/proc/sys/...`;
- целое значение нормализуется как signed base10 без ведущих нулей;
- VALUE/PASS, VALUE/FAIL, NOT_FOUND и ERROR;
- ошибка чтения не создаёт неструктурированный stderr (P-01);
- registry содержит current строки для `sysctl`, `file-mode-owner` и
  `kernel-cmdline`; все contract/binding/implementation SHA совпадают.

`test_product_generator.py` — regression предыдущего `generate-product-check-v1.py`, current adapter semantics и current unified CLI v2:

- current `CONTROL-MANIFEST.tsv` определяет population динамически и проверяется по SHA;
- `ADAPTER-REGISTRY.tsv` является единственным источником выбора adapter;
- registry SHA для semantic contract / binding / implementation проверяются;
- deterministic render одинаковых входов даёт одинаковые bytes;
- current controls маршрутизируются по `parameter.kind` через tracked `ADAPTER-REGISTRY.tsv`;
- synthetic `file-mode-owner` подтверждает dispatch через второй adapter;
- unknown kind отклоняется fail-closed;
- generated CHECK проходит `bash -n`, `--help`, `--build-info`,
  `--provenance` и usage-RC;
- generated bytes проходят defense-in-depth scan известных mutating tokens; scan не считается формальным доказательством отсутствия произвольной shell-mutation capability; APPLY/RESTORE остаются NOT_IMPLEMENTED stubs.
Current-population regression also pins the exact `sysctl eq` semantics for `SRC-0030`, `SRC-0031`, `SRC-0036`–`SRC-0039` and `SRC-0040 / fs.suid_dumpable=0`, while requiring `SRC-0034` to remain absent until its procedural `после тестирования` qualifier is represented.

Current sysctl regression covers `eq` and unbounded signed-integer `ge`; `ge` is used only for source clauses that explicitly define a lower bound.

`test_product_generator.py` также покрывает новый
`product-kernel-cmdline-check-v1`: exact `/proc/cmdline` token semantics,
`eq`/`present`, absence as VALUE/FAIL, conflict as ERROR, adapter selftest,
9 controls для 7 source rows и отсутствие `SRC-0026`/`SRC-0034` до отдельной
семантики.

Дополнительный `UnifiedCliArtifact` в том же test-file проверяет byte-exact parity tracked `securelinux-policy.sh` с `generate-product-check-v2.py`, fixed-column pretty output, raw/JSON, `--failed`, metadata/provenance и fail-closed APPLY/RESTORE stubs.

`test_product_generator.py` также содержит positive/negative/robustness fixtures для SRC-0002 `sshd-root-login`: main `PermitRootLogin no`, whitespace/`=`/quoted/CRLF формы, malformed relevant directives, восстановление Match-scope между Include-файлами, lexical glob order, `#` внутри pathname, quoted Include, function-shadowed `compgen`, sort failure, newline pathname, Include precedence, Match ambiguity, syntax/effective failure, missing и symlink config.

`test_product_generator.py` также содержит SRC-0003 `pam-wheel-access` fixtures: root-only и explicit-user PASS, arbitrary numeric GID, CRLF/continuation/@include, redundant exact rule, missing PAM/wheel/root definitive FAIL, authority exact-set comparison, missing/malformed authority, non-source pam_wheel options/path/control, duplicate/malformed wheel, symlink и NOT_FOUND fail-closed cases.

`test_product_generator.py` также содержит SRC-0004 `sudoers-reviewed-policy` fixtures: exact active policy tree PASS; pathset/byte drift VALUE/FAIL; missing/malformed/symlink/binary authority и `visudo`/closure ambiguity ERROR; отдельные negative-control cases доказывают, что slash-named `/usr/bin/od`, `/usr/bin/sha256sum` и `/usr/sbin/visudo` functions не подменяют observation. Дополнительные regressions закрывают два independently reproduced fail-open случая: nonstructural authority control byte `0x01` и raw `visudo` stdout `<path><NUL>: parsed OK` теперь дают `ERROR` до line parsing. Approved policy не выводится из `%sudo`, `%wheel`, `SUDO_USER`, donor или VM defaults. Всего для SRC-0004 — 10 targeted fixtures.

`test_product_generator.py` также содержит SRC-0006 `running-process-paths-write-protection` fixtures: positive dynamic population; file/parent write `FAIL`; deleted/missing maps `ERROR`; executable file-backed mappings без basename-эвристики; proc octal path decoding; full maps grammar, включая запрет Unicode-цифр в inode, и maps-declared dev/inode binding; per-PID maps completeness; ASCII-decimal PID/starttime, полный 52-field `/proc/<pid>/stat` layout без пустого/сдвинутого starttime и `/proc/stat` process-counter closure; same-PID exe/maps drift; file/parent snapshot drift — `ERROR`.

- External-tool shadowing regressions: slash-named `/usr/bin/stat`/`/usr/bin/od` functions не подменяют adapter observations; unified executable дополнительно имеет `#!/bin/bash -p`, и regression с экспортированной функцией `command` доказывает, что environment function не импортируется в compliance execution.
- PAM raw-byte regression distinguishes canonical CRLF from bare CR at EOF and rejects NUL before Bash line parsing.
