# Тесты product-v1

`test_file_mode_owner_adapter.py` — механические тесты
`product/adapters/product-file-mode-owner-check-v2.py`:

- emitted bash не содержит мутирующих примитивов;
- fail-closed отказ для `owner` / `group` / `owner_group`, для op вне
  `eq` / `bits-clear`, для маски `0000`, для не-четырёхзначного значения,
  для относительного locator, для locator с кавычкой, переводом строки
  или подстановкой, для control_id вне `[A-Za-z0-9._-]`;
- связка adapter JSON <-> реализация <-> семантический контракт по SHA-256;
- поведение на фикстурах: VALUE (в том числе setuid-биты и режим 0000),
  NOT_FOUND только при доказанном отсутствии, ERROR для висячей ссылки,
  цикла ссылок, непроходимого родителя и отсутствия `stat`;
- целевой файл не изменяется.

Два ordinary-user permission regressions выполняются и при запуске suite от root: дочерний Bash запускается с непривилегированными UID/GID, поэтому relevant assertion не может молча скрыться за EUID root.

`test_sysctl_adapter.py` — механические тесты
`product/adapters/product-sysctl-check-v2.py` и `product/ADAPTER-REGISTRY.tsv`:

- отдельная product identity, historical `sysctl-check-v1` не изменяется;
- только `parameter.kind=sysctl`, `locator=sysctl`; current v2 поддерживает
  `eq` и source-faithful integer lower-bound `ge`;
- dotted sysctl key преобразуется в `/proc/sys/...`;
- целое значение нормализуется как signed base10 без ведущих нулей;
- VALUE/PASS, VALUE/FAIL, NOT_FOUND и ERROR;
- каждый ERROR fixture проверяет непустой стабильный reason-code `domain:reason`; production `ERROR` с `-` запрещён, при этом `NOT_FOUND` сохраняет `-`;
- ошибка чтения не создаёт неструктурированный stderr (P-01);
- registry содержит current строки для `sysctl`, `file-mode-owner` и
  `kernel-cmdline`; все contract/binding/implementation SHA совпадают.

`test_product_generator.py` — regression предыдущего `generate-product-check-v1.py`, current adapter semantics и current unified CLI v2:

- current `CONTROL-MANIFEST.tsv` определяет population динамически и проверяется по SHA;
- `ADAPTER-REGISTRY.tsv` является единственным источником выбора adapter;
- registry SHA для semantic contract / binding / implementation проверяются;
- для `ERROR` проверяется не только форма `domain:reason`, но и exact branch→reason binding на воспроизводимых failure-mode fixtures;
- deterministic render одинаковых входов даёт одинаковые bytes;
- current controls маршрутизируются по `parameter.kind` через tracked `ADAPTER-REGISTRY.tsv`;
- synthetic `file-mode-owner` подтверждает dispatch через второй adapter;
- unknown kind отклоняется fail-closed;
- generated CHECK проходит `bash -n`, `--help`, `--build-info`,
  `--provenance` и usage-RC;
- generated bytes проходят defense-in-depth scan известных mutating tokens; scan не считается формальным доказательством отсутствия произвольной shell-mutation capability; APPLY ограничен `SRC-0001_ONLY` и проверяется отдельными usage/dry-run/report/registry regressions; пользовательский `--restore` отсутствует, operational RESTORE не планируется к реализации.
Регрессия текущей population также закрепляет точную семантику `sysctl eq` для `SRC-0030`, `SRC-0031`, `SRC-0036`–`SRC-0039` и `SRC-0040 / fs.suid_dumpable=0`. `SRC-0034` состоит из одного control `kernel.randomize_va_space=2`; «после тестирования» — порядок действий администратора, не объект проверки.

Текущий sysctl regression покрывает `eq` и unbounded signed-integer `ge`; `ge` используется только для source clauses, которые явно задают lower bound.

`test_product_generator.py` также покрывает новый
`product-kernel-cmdline-check-v1`: точная семантика токенов `/proc/cmdline`,
`eq`/`present`, отсутствие как VALUE/FAIL, конфликт как ERROR, selftest адаптера,
Исторический kernel-cmdline batch сначала содержал 9 controls для 7 source rows и не включал `SRC-0026`/`SRC-0034`; current SRC-0034 semantics теперь отдельно представлены двухконтрольным closure.

Дополнительный `UnifiedCliArtifact` в том же test-file проверяет byte-exact parity tracked `securelinux-policy.sh` с `generate-product-check-v2.py`, exact primary 7-environment clean-reference classifier из `SUPPORTED-PLATFORMS.tsv` плюс отдельный Ubuntu 24.04 Desktop `FIELD_COMPATIBILITY` из `FIELD-COMPATIBILITY-DESKTOPS.tsv`, fail-closed distinct `UNSUPPORTED_PROFILE` / `UNSUPPORTED_TYPE` / `UNSUPPORTED_PLATFORM`, fixed-column pretty output с runtime OS/arch/profile-or-type/platform, `SLP-PLATFORM-V1` raw identity, JSON `platform`, `--failed`, metadata/provenance, `SRC-0001` APPLY dispatch/usage/report contract и отсутствие `--restore`; RESTORE при этом не является future feature.

`test_product_generator.py` также содержит positive/negative/robustness fixtures для SRC-0002 `sshd-root-login`: main `PermitRootLogin no`, whitespace/`=`/quoted/CRLF формы, malformed relevant directives, восстановление Match-scope между Include-файлами, lexical glob order, `#` внутри pathname, quoted Include, function-shadowed `compgen`, sort failure, newline pathname, Include precedence, Match ambiguity, syntax/effective failure, missing и symlink config.

`test_product_generator.py` также содержит SRC-0003 `pam-wheel-access` fixtures: root-only и explicit-user PASS, literal GID 10 + non-10 FAIL, пустое group-password field как допустимый non-predicate, CRLF/continuation, include-after-rule PASS, prior `auth/-auth` include/sufficient/substack/extended-stack ERROR, redundant exact rule, missing PAM/wheel/root definitive FAIL, authority exact-set comparison, missing/malformed authority, non-source pam_wheel options/path/control, duplicate/malformed wheel, symlink и NOT_FOUND fail-closed cases. Unified collector regression отдельно доказывает, что contract-valid `NOT_FOUND/FAIL` для SSH/PAM остаётся `NONCOMPLIANT`, а не превращается в `CHECK_INTERNAL_ERROR`.

`test_product_generator.py` также проверяет, что 2.5.11 в сгенерированном CLI — одна sysctl-функция, результат которой зависит только от значения `kernel.randomize_va_space`, а выведенный `tested-setting-attestation` остаётся только historical bytes вне `ADAPTER-REGISTRY.tsv`.

`test_product_generator.py` также содержит SRC-0004 `sudoers-reviewed-policy` fixtures: exact active policy tree PASS; pathset/byte drift VALUE/FAIL; missing/malformed/symlink/binary authority и `visudo`/closure ambiguity ERROR; отдельные negative-control cases доказывают, что slash-named `/usr/bin/od`, `/usr/bin/sha256sum` и `/usr/sbin/visudo` functions не подменяют observation. Дополнительные regressions закрывают два воспроизведённых fail-open случая: nonstructural authority control byte `0x01` и raw `visudo` stdout `<path><NUL>: parsed OK` теперь дают `ERROR` до line parsing. Branch-specific regressions отдельно различают unexpected/duplicate closure и symlink/missing/nonregular member paths по стабильному reason-code. Approved policy не выводится из `%sudo`, `%wheel`, `SUDO_USER`, donor или VM defaults.


`test_product_generator.py` также содержит 25 targeted fixtures для SRC-0007 `cron-command-paths-write-protection`: empty population; direct system/user absolute command; direct и `run-parts` target write violation; explicit root `PATH`; non-root/bare PATH ambiguity; `%` stdin boundary; active/ignored `/etc/cron.d` names; unknown system user; shell expansion/redirection; interpreter и versioned-interpreter rejection; absolute/additional launcher rejection; symlink-alias interpreter/wrapper/`run-parts` classification; hardlink-alias interpreter/`run-parts` fail-closed regressions; non-`/bin/sh` `SHELL`; missing final LF; adapter selftest и wrong-contract rejection.

`test_product_generator.py` также содержит SRC-0006 `running-process-paths-write-protection` fixtures: positive dynamic population; file/parent write `FAIL`; deleted/missing maps `ERROR`; executable file-backed mappings без basename-эвристики; proc octal path decoding; full maps grammar, включая запрет Unicode-цифр в inode, и maps-declared dev/inode binding; per-PID maps completeness; ASCII-decimal PID/starttime, полный 52-field `/proc/<pid>/stat` layout без пустого/сдвинутого starttime и `/proc/stat` process-counter closure; same-PID exe/maps drift; file/parent snapshot drift — `ERROR`.

- Regressions подмены внешних инструментов: slash-named `/usr/bin/stat`/`/usr/bin/od` functions не подменяют adapter observations; unified executable дополнительно имеет `#!/bin/bash -p`, и regression с экспортированной функцией `command` доказывает, что environment function не импортируется в compliance execution.
- PAM raw-byte regression отличает canonical CRLF от bare CR в EOF и отклоняет NUL до разбора строк Bash.
- Platform preflight regressions проверяют invalid/truncated UTF-8 в `/etc/os-release`, valid Unicode identity, single/double quoted `ID`/`VERSION_ID`/`PRETTY_NAME` и shell-style escaping без выполнения файла через `source`.

- Synthetic user-crontab fixtures не выполняют `chown`: владелец файла остаётся UID/GID текущего test runner; отдельная non-root bare-command семантика проверяется system-cron fixture с synthetic nonzero UID. Это test-only portability и не меняет production UID=0 semantics для `/etc/cron.d`.


`test_product_generator.py` также содержит 28 targeted fixtures для SRC-0008 `sudo-root-command-files-protection`: exact root-owner/go-w PASS; owner и group/other-write FAIL; final symlink target; root-only invoker и explicit non-root runas exclusion; host-qualified / ambiguous invoker-runas membership fail-closed; unbounded/dynamic forms; command negation/digest applicability ERROR; reviewed-policy drift; `cvtsudoers` failure/malformed JSON; target drift; spaced executable pathname без first-word truncation; ambiguous executable/argument boundary ERROR; shebang execution chain ERROR; adapter selftest и wrong-contract rejection. Fixtures не используют `chown`; owner mismatch моделируется через synthetic logical-root UID, production UID=0 semantics не меняется.
  Дополнительно: `Defaults runas_default`, override `case_insensitive_user` и command `NOTBEFORE/NOTAFTER` обязаны давать `ERROR`; alternate-case `ROOT` проверяется по default case-insensitive sudo semantics.

### Ретроспективные regressions FSTEC-40

Текущий набор tests дополнительно фиксирует robustness-границы всех 40 строк: для SRC-0006 group-class parent `wx` даёт ERROR (одного mode недостаточно, чтобы доказать применимость principal/ACL), однозначный other `wx` даёт FAIL, а write-without-search не ошибочно трактуется как право записи directory-entry. Набор tests также фиксирует независимо воспроизведённые ретроспективные counterexamples: для SRC-0001 NUL/CR во входных данных состояния учётной записи даёт ERROR; для SRC-0003 проверяются literal GID 10 группы wheel, поле пароля группы, не являющееся предикатом, и неоднозначность pre-rule `auth/-auth` PAM short-circuit/include; для SRC-0005 каталог с совпадающим mode даёт ERROR; для SRC-0011 canonical direct-only population `/var/spool/cron/crontabs` исключает посторонние объекты parent spool и соседние `atd`, сохраняя fail-closed обход admitted root; для SRC-0012 population исполняемых файлов root PATH исключает обычные неисполняемые данные; для SRC-0013 SUID/SGID-файлы на `nosuid` остаются в population; для SRC-0014 проверяются service accounts, не перечисленная `.zsh_history`, Bash `.bash_login`, обнаружение config/autoload/history Nushell, стандартных config/history Xonsh/Elvish и отсутствие захвата `.vimrc`; для SRC-0015 — домашние каталоги service accounts. Эти tests являются negative-control evidence исправленных current adapter identities.
