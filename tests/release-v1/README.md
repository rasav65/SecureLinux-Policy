# release-v1 tests

The test suite runs the mandatory real Draft202012Validator gate and also
injects a missing-dependency condition to prove that release validation fails
closed instead of skipping.
